def pal_in_drome(word):
    ''' Return True if word is a palindrome, False if not.'''
    if len(word) <= 1:
        return True
    else:
        return word[0] == word[-1] and palindrome(word[1:-1])
        
